name: build
jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    on: push
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2

      - run: bun install
      - run: bun run build
